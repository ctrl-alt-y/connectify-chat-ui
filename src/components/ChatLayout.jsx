
const ChatLayout = ({ children }) => {
  return (
    <div className="w-full max-w-7xl mx-auto bg-white shadow-xl rounded-xl overflow-hidden border border-gray-200/50">
      {children}
    </div>
  );
};

export default ChatLayout;
